extends base.pug
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search by title", name="keyword", type="test")
        input(type="submit", value="Search now")

    each video in videos
        +video(video)